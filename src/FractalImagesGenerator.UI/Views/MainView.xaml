<Window x:Class="FractalImagesGenerator.UI.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FractalImagesGenerator.UI" 
        mc:Ignorable="d"
        Height="600" Width="1200">
    
    <Grid>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0" Background="DimGray">

            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Label Grid.Row="0" Content="Fractal Images Generator" FontSize="20" Padding="20"/>

            <Border Grid.Row="1" BorderBrush="Black" Margin="10">
                <StackPanel Margin="10">
                    <Button Margin="0, 10, 0, 10" Height="60" Content="Mandelbrot Set" />
                    <Button Margin="0, 10, 0, 10" Height="60" Content="Julia Set"/>
                </StackPanel>
            </Border>

        </Grid>

        <ContentControl Grid.Column="1" Content="{Binding CurrentPage}"/>

    </Grid>
</Window>
